const APP_PREFIX=window.location.pathname.startsWith('/v2-preview')?'/v2-preview':'';const API_BASE=window.location.origin+APP_PREFIX;
const state={quiz:null,personalities:null,products:null,media:null,step:0,answers:[],attemptId:'',result:null,sessionId:getOrCreate('type_me_session_id',()=>`s_${cryptoRandom(12)}`),source:new URLSearchParams(location.search).get('source')||'direct',campaign:new URLSearchParams(location.search).get('campaign')||'',schoolId:new URLSearchParams(location.search).get('school_id')||new URLSearchParams(location.search).get('school')||'',creatorId:new URLSearchParams(location.search).get('creator_id')||'',referrerId:new URLSearchParams(location.search).get('referrer_id')||new URLSearchParams(location.search).get('referrer')||'',shareId:new URLSearchParams(location.search).get('share_id')||'',color:'黑',size:'M',currentOrderId:''};
function cryptoRandom(len=12){const a=new Uint8Array(len);crypto.getRandomValues(a);return[...a].map(x=>x.toString(16).padStart(2,'0')).join('')}
function getOrCreate(key,factory){let v=localStorage.getItem(key);if(!v){v=factory();localStorage.setItem(key,v)}return v}
function $(id){return document.getElementById(id)}
function showPage(name){document.querySelectorAll('.page').forEach(el=>el.classList.remove('active'));$(`${name}Page`).classList.add('active');if($('homeBtn'))$('homeBtn').hidden=name==='home';document.body.dataset.page=name;scrollTo({top:0,behavior:'auto'})}
function toast(msg){$('toast').textContent=msg;$('toast').classList.add('show');setTimeout(()=>$('toast').classList.remove('show'),2200)}
async function postJSON(url,data={}){const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.message||`HTTP ${r.status}`);return j}
async function track(eventName,extra={}){try{await postJSON(`${API_BASE}/api/events/track.php`,{event_name:eventName,session_id:state.sessionId,source:state.source,campaign:state.campaign,school_id:state.schoolId,creator_id:state.creatorId,referrer_id:state.referrerId,share_id:state.shareId,primary_personality:state.result?.primary?.key||'',secondary_personality:state.result?.secondary?.key||'',...extra})}catch(e){console.debug('track failed',eventName,e.message)}}
function assetUrl(path){return path?`${API_BASE}/${String(path).replace(/^\/+/, '')}`:''}
async function boot(){const [cfg,dynamic]=await Promise.all([fetch(`${API_BASE}/config/v2.json`,{cache:'no-store'}).then(r=>r.json()),fetch(`${API_BASE}/api/personality/types.php`,{cache:'no-store'}).then(r=>r.ok?r.json():null).catch(()=>null)]);state.quiz=cfg.quiz;state.personalities=cfg.personality_config;if(dynamic?.personalities)state.personalities={...state.personalities,personalities:dynamic.personalities};state.products=cfg.product_config;state.media=cfg.media_config||{};renderHomeVisual();renderPreview();bind();await track('landing_view');if(state.shareId){await track('share_open');try{await postJSON(`${API_BASE}/api/share/open-v2.php`,{share_id:state.shareId,referrer_id:state.referrerId,session_id:state.sessionId,source:'share'})}catch(e){}}loadDistribution()}
function bind(){
  $('startBtn').onclick=startQuiz;
  $('prevBtn').onclick=prevQuestion;
  $('homeBtn').onclick=()=>showPage('home');
  $('brandBtn').onclick=()=>showPage('home');
  $('dormTopBtn').onclick=openDormEntry;
  $('homeDormBtn').onclick=openDormEntry;
  $('viewProductBtn').onclick=()=>{showPage('product');const product=state.products.products[state.result.primary.key];track('product_view',{product_id:product?.product_id||''})};
  $('productBackBtn').onclick=()=>showPage('result');
  $('generateCardBtn').onclick=generateIdentityCard;
  $('shareBtn').onclick=shareResult;
  $('resultDormBranch').onclick=openDormEntry;
  $('resultProductBranch').onclick=()=>$('viewProductBtn').click();
  $('buyBtn').onclick=openCheckout;
  $('closeCheckoutBtn').onclick=()=>$('checkoutModal').classList.remove('open');
  $('payBtn').onclick=checkout;
  $('closeNativeBtn').onclick=()=>$('nativePayModal').classList.remove('open')
}
function openDormEntry(){
  if(!state.result){toast('先完成测试，再创建宿舍挑战');startQuiz();return}
  showPage('result');setTimeout(()=>$('dormChallengeBlock').scrollIntoView({behavior:'smooth',block:'center'}),30)
}
function renderHomeVisual(){const home=state.media?.home||{},lead=home.lead||(home.supporting||[])[0];$('homeVisual').innerHTML=lead?`<img src="${assetUrl(lead)}" alt="TYPE ME 校园人格主视觉" width="1200" height="750" fetchpriority="high">`:'';if(state.media?.dorm){$('dormEntryVisual').src=assetUrl(state.media.dorm);$('homeDormVisual').src=assetUrl(state.media.dorm)}}
function renderPreview(){
  const keys=Object.keys(state.personalities.personalities).sort((a,b)=>String(state.personalities.personalities[a].type).localeCompare(String(state.personalities.personalities[b].type)));
  $('previewGrid').innerHTML=keys.map(k=>{const p=state.personalities.personalities[k],media=state.media?.personalities?.[k]||{};return`<article class="preview-card" style="--preview-accent:${p.accent}"><img src="${assetUrl(media.main)}" alt="${media.alt||`${p.type} ${p.cn}`}" width="1200" height="1500" loading="lazy"><div class="preview-card-copy"><div class="preview-type">${p.type}</div><div class="preview-name">${p.cn}</div><div class="preview-en">${p.en}</div></div></article>`}).join('')
}
async function loadDistribution(){try{const r=await fetch(`${API_BASE}/api/test/distribution-v2.php`);if(!r.ok)return;const d=await r.json();if((d.total||0)<100)return;$('distributionBlock').hidden=false;$('distributionContent').innerHTML=(d.items||[]).slice(0,5).map(x=>{const p=state.personalities.personalities[x.key];return`<div style="display:flex;justify-content:space-between;padding:8px 0;border-bottom:1px solid #ddd"><b>${p?.cn||x.key}</b><span>${x.percent}%</span></div>`}).join('')}catch(e){}}
async function startQuiz(){state.step=0;state.answers=[];try{const d=await postJSON(`${API_BASE}/api/test/start-v2.php`,{session_id:state.sessionId,source:state.source,campaign:state.campaign,school_id:state.schoolId,creator_id:state.creatorId,referrer_id:state.referrerId,share_id:state.shareId});state.attemptId=d.attempt_id||''}catch(e){state.attemptId=`local_${Date.now()}_${cryptoRandom(4)}`}await track(state.shareId?'viral_test_start':'test_start',{attempt_id:state.attemptId});showPage('quiz');renderQuestion()}
function renderQuestion(){const q=state.quiz.questions[state.step];$('quizCounter').textContent=`${state.step+1} / ${state.quiz.questions.length}`;$('progressFill').style.width=`${((state.step+1)/state.quiz.questions.length)*100}%`;$('questionText').textContent=q.text;$('options').innerHTML=q.options.map((o,i)=>`<button class="option ${state.answers[state.step]===i?'selected':''}" data-index="${i}">${o.text}</button>`).join('');[...$('options').querySelectorAll('.option')].forEach(btn=>btn.onclick=()=>chooseAnswer(Number(btn.dataset.index)));$('prevBtn').style.visibility=state.step===0?'hidden':'visible';track('question_view',{question_id:q.id,question_index:state.step+1})}
async function chooseAnswer(index){const q=state.quiz.questions[state.step];state.answers[state.step]=index;[...$('options').children].forEach((el,i)=>el.classList.toggle('selected',i===index));track('question_answer',{question_id:q.id,question_index:state.step+1,answer_index:index});if(state.attemptId&&!state.attemptId.startsWith('local_'))postJSON(`${API_BASE}/api/test/answer-v2.php`,{attempt_id:state.attemptId,question_id:q.id,answer_index:index}).catch(()=>{});await new Promise(r=>setTimeout(r,170));if(state.step<state.quiz.questions.length-1){state.step++;renderQuestion()}else await completeQuiz()}
function prevQuestion(){if(state.step>0){state.step--;renderQuestion()}}
async function completeQuiz(){if(state.answers.length!==state.quiz.questions.length){toast('还有题目没答完');return}let data;try{data=await postJSON(`${API_BASE}/api/test/complete-v2.php`,{attempt_id:state.attemptId,answers:state.answers,session_id:state.sessionId,source:state.source,campaign:state.campaign,school_id:state.schoolId,creator_id:state.creatorId,referrer_id:state.referrerId,share_id:state.shareId})}catch(e){data=scoreLocally(state.answers);data.sample={total:0,personality_count:0,percent:null}}state.result=data;await track(state.shareId?'viral_test_complete':'test_complete');renderResult();showPage('result');window.dispatchEvent(new CustomEvent('type-me:result',{detail:state.result}));await track('result_view')}
function scoreLocally(answers){const scores=Object.fromEntries(Object.keys(state.personalities.personalities).map(k=>[k,0]));const last4=Object.fromEntries(Object.keys(scores).map(k=>[k,0]));answers.forEach((idx,qi)=>{const weights=state.quiz.questions[qi].options[idx].weights||{};for(const[k,v]of Object.entries(weights)){scores[k]=(scores[k]||0)+v;if(qi>=8)last4[k]=(last4[k]||0)+v}});const seed=answers.join(',');const ranked=Object.keys(scores).sort((a,b)=>scores[b]-scores[a]||last4[b]-last4[a]||stable(a,seed)-stable(b,seed));const make=key=>{const p=state.personalities.personalities[key],max=36,n=Math.min(1,scores[key]/max);const metrics=p.metrics.map((name,i)=>({name,value:Math.max(8,Math.min(97,Math.round(36+n*50+p.metric_bias[i])))}));return{key,...p,score:scores[key],metrics}};return{primary:make(ranked[0]),secondary:make(ranked[1]),scores}}
function stable(key,seed){let h=2166136261;for(const c of`${seed}|${key}`){h^=c.charCodeAt(0);h=Math.imul(h,16777619)}return h>>>0}
function renderResult(){
  const p=state.result.primary,s=state.result.secondary,media=state.media?.personalities?.[p.key]||{};
  document.documentElement.style.setProperty('--accent',p.accent||'#f3ff38');$('resultHero').style.setProperty('--accent',p.accent||'#f3ff38');
  $('resultType').textContent=p.type;$('resultName').textContent=p.cn;$('resultEn').textContent=p.en;$('resultCore').textContent=p.main_meme||p.core;$('resultDescription').textContent=p.description;
  $('resultVisual').src=assetUrl(media.main);$('resultVisual').alt=media.alt||`${p.type} ${p.cn}校园穿搭视觉`;
  $('cardVisual').src=assetUrl(media.main);
  $('resultGallery').innerHTML=[['日常穿搭',media.front,'story-lead'],['背面设计',media.back,'story-support'],['校园场景',media.scene,'story-support']].filter(x=>x[1]).map(([label,src,kind])=>`<figure class="${kind}"><img src="${assetUrl(src)}" alt="${p.type} ${p.cn}${label}" width="1200" height="1500" loading="lazy"><figcaption>${label}</figcaption></figure>`).join('');
  $('resultVisualNote').textContent=state.media?.visual_disclaimer||'';
  $('metrics').innerHTML=(p.metrics||[]).map(m=>`<div class="metric"><div class="metric-top"><span>${m.name}</span><span>${m.value}%</span></div><div class="metric-bar"><span style="width:${m.value}%"></span></div></div>`).join('');
  $('secondaryType').textContent=s.type;$('secondaryName').textContent=s.cn;$('secondaryCore').textContent=s.core;$('skillText').textContent=p.skill;$('weaknessText').textContent=p.weakness;
  const sample=state.result.sample||{};if((sample.total||0)>=100&&sample.percent!==null){$('populationStat').textContent=`${sample.percent}%`;$('populationNote').textContent=`当前已完成测试用户中，${p.cn}占比。`}else{$('populationStat').textContent='样本正在积累中';$('populationNote').textContent='完成样本不足 100 时不展示比例。'}
  $('cardType').textContent=p.type;$('cardName').textContent=p.cn;$('cardEn').textContent=p.en;$('cardCore').textContent=p.identity_card_meme||p.core;
  const generatedCard=$('generatedCardWrap');if(generatedCard)generatedCard.remove();
  document.querySelector('.identity-section')?.classList.remove('has-generated-card');
  $('cardStatus').textContent='二维码会绑定本次分享归因。';
  $('friendShareCopy').textContent=p.friend_meme||'让朋友继续测试';
  $('productTeaserVisual').src=assetUrl(media.front||media.main);$('productTeaserVisual').alt=`${p.type} ${p.cn} T 恤视觉`;$('productTeaserName').textContent=`${p.type} ${p.cn} T 恤`;$('tshirtTeaserCopy').textContent=p.tshirt_copy||p.core;
  renderProduct()
}
function renderProduct(){
  const p=state.result.primary,product=state.products.products[p.key],d=state.products.details||{},labels=state.products.size_labels||{};
  const availableColors=product?.available_colors||state.products.colors||[];
  const displayColor=product?.display_color||availableColors[0]||'';
  const colors=state.products.sales_enabled?availableColors:[displayColor].filter(Boolean);if(!colors.includes(state.color))state.color=colors[0]||'';
  const sizes=(state.products.sizes||[]).filter(size=>Number(product?.stock_by_size?.[size]||0)>0);if(!sizes.includes(state.size))state.size=sizes[0]||'';
  $('productName').textContent=product?.name||`${p.type} · ${p.cn}人格 T 恤`;$('productIdentityCopy').textContent=p.tshirt_copy||p.core;
  const colorHex={'黑':'#111111','白':'#ffffff','灰':'#a9a9a9'};
  $('colorChips').innerHTML=colors.map(c=>`<button class="chip color-chip ${c===state.color?'active':''}" data-v="${c}" aria-pressed="${c===state.color}"><span class="color-dot" style="--chip-color:${colorHex[c]||'#dddddd'}"></span><span>${c}</span></button>`).join('');
  $('sizeChips').innerHTML=sizes.map(s=>`<button class="chip size-chip ${s===state.size?'active':''}" data-v="${s}" aria-pressed="${s===state.size}" aria-label="${labels[s]||s}，库存 ${product.stock_by_size[s]} 件">${labels[s]||s}</button>`).join('');
  $('productInventory').textContent=state.products.sales_enabled
    ?`当前批次：${availableColors.join(' / ')} · 合计 ${Number(product?.stock_total||0)} 件。`
    :`当前视觉展示：${displayColor||'待确认'}；正式开售颜色以真实商品与 SKU 库存表为准。`;
  const specs=[["面料",d.material],["克重",d.weight],["纱支",d.yarn_count],["版型",d.fit],["领口",d.collar],["印花",d.print_method],["缩水率",d.shrinkage_rate!=null?`${Math.round(Number(d.shrinkage_rate)*100)}%`:''],["抗起球",d.pilling],["色牢度",d.colorfastness],["透度",d.opacity]].filter(x=>x[1]);
  $('productSpecs').innerHTML=specs.map(x=>`<div><span>${x[0]}</span><b>${x[1]}</b></div>`).join('');
  $('sizeGuideRows').innerHTML=state.products.sizes.map(size=>{const x=d.size_spec_cm?.[size]||{};return `<tr><td><b>${labels[size]||size}</b></td><td>${x.bust??'-'}</td><td>${x.length??'-'}</td><td>${x.shoulder??'-'}</td></tr>`}).join('');
  const f=d.models?.female,m=d.models?.male;$('modelRef').innerHTML=`<b>真人尺码参考</b><span>女模：${f?.height_cm||'-'}cm / ${f?.weight_kg||'-'}kg / ${f?.size||'-'}</span><span>男模：${m?.height_cm||'-'}cm / ${m?.weight_kg||'-'}kg / ${m?.size||'-'}</span>`;
  $('careList').innerHTML=(d.care||[]).map(item=>`<li>${item}</li>`).join('');
  $('productDetailPhotos').innerHTML=(state.media?.product_details||[]).map(item=>`<figure><img src="${assetUrl(item.image)}" alt="${item.alt||item.label}" width="1200" height="1200" loading="lazy"><figcaption>${item.label}</figcaption></figure>`).join('');
  const fulfill=d.fulfillment||{};$('fulfillmentNote').innerHTML=`<b>发货与售后</b><span>正常订单 ${fulfill.normal_dispatch||'-'} 发出；订制约 ${fulfill.custom_dispatch||'-'}。</span><span>${fulfill.shipping||''}</span><span>${fulfill.size_exchange||''}</span><span>${fulfill.quality_return||''}</span>`;
  const buy=$('buyBtn');buy.disabled=!state.products.sales_enabled;buy.textContent=state.products.sales_enabled?'穿上我的 TYPE · ¥129':state.products.sales_status;
  $('productPendingNote').textContent=`正式开售前仍需确认：${(d.pending_fields||[]).join('、')}。${state.media?.visual_disclaimer||''}`;
  [...$('colorChips').children].forEach(b=>b.onclick=()=>{state.color=b.dataset.v;renderProduct();track('color_select',{product_id:product.product_id,color:state.color})});
  [...$('sizeChips').children].forEach(b=>b.onclick=()=>{state.size=b.dataset.v;renderProduct();track('size_select',{product_id:product.product_id,size:state.size})});
}
async function generateIdentityCard(){await track('identity_card_generate');$('cardStatus').textContent='身份证内容已生成。PNG 服务端渲染待服务器图像扩展确认后接入；当前不会伪装成已生成图片。';toast('人格身份证内容已生成')}
async function shareResult(){await track('share_click');try{const p=state.result.primary,d=await postJSON(`${API_BASE}/api/share/create-v2.php`,{session_id:state.sessionId,primary_personality:p.key,secondary_personality:state.result.secondary.key});const link=d.share_url;state.shareId=d.share_id||state.shareId;await configureWechatShare(link);if(navigator.share)await navigator.share({title:`TYPE ME｜我是${p.cn}`,text:p.share_copy||p.core,url:link});else if(navigator.clipboard){await navigator.clipboard.writeText(link);toast('分享链接已复制')}else prompt('复制这个链接分享给朋友',link)}catch(e){toast(`分享链接生成失败：${e.message}`)}}
async function configureWechatShare(link){if(!/MicroMessenger/i.test(navigator.userAgent)||typeof wx==='undefined')return;try{const r=await fetch(`${API_BASE}/api/wechat/jssdk-sign.php?url=${encodeURIComponent(location.href.split('#')[0])}`),d=await r.json();if(d.code!==0)return;wx.config({debug:false,appId:d.appId,timestamp:d.timestamp,nonceStr:d.nonceStr,signature:d.signature,jsApiList:['updateAppMessageShareData','updateTimelineShareData']});wx.ready(()=>{const p=state.result.primary,shareData={title:`TYPE ME｜我是${p.cn}`,desc:p.share_copy||p.core,link,imgUrl:`${API_BASE}/share-cover.jpg`};wx.updateAppMessageShareData(shareData);wx.updateTimelineShareData(shareData)})}catch(e){}}
function openCheckout(){if(!state.products.sales_enabled){toast(state.products.sales_status||'暂未开放购买');return}const product=state.products.products[state.result.primary.key];track('add_cart',{product_id:product.product_id,sku_id:`${product.product_id}-${state.color}-${state.size}`});$('checkoutModal').classList.add('open')}
function isWechat(){return/MicroMessenger/i.test(navigator.userAgent)}
function getCookie(name){return document.cookie.split('; ').find(x=>x.startsWith(name+'='))?.split('=').slice(1).join('=')||''}
function ensureOpenid(){if(!isWechat())return'';const openid=getCookie('wx_openid');if(openid)return openid;location.href=`${API_BASE}/api/wechat/oauth.php?return=${encodeURIComponent(location.href)}`;return''}
async function checkout(){const receiver_name=$('receiverName').value.trim(),receiver_phone=$('receiverPhone').value.trim(),receiver_address=$('receiverAddress').value.trim();if(!receiver_name||!receiver_phone||!receiver_address){toast('请完整填写收货信息');return}const product=state.products.products[state.result.primary.key],openid=ensureOpenid();if(isWechat()&&!openid)return;await track('checkout_start',{product_id:product.product_id,sku_id:`${product.product_id}-${state.color}-${state.size}`});try{const r=await fetch(`${API_BASE}/api/pay/unified-order.php`,{method:'POST',headers:{'Content-Type':'application/json',...(openid?{'X-User-Openid':openid}:{})},body:JSON.stringify({amount:12900,description:product.name,items:[{name:product.name,price:129,qty:1,type:'personality_tee',personality:state.result.primary.key,color:state.color,size:state.size}],scene:isWechat()?'JSAPI':'NATIVE',receiver_name,receiver_phone,receiver_address,design_type:'personality_tee',quiz_result:`${state.result.primary.type} ${state.result.primary.cn}`})});const d=await r.json();if(!r.ok||d.code<0)throw new Error(d.message||'创建订单失败');state.currentOrderId=d.out_trade_no;if(d.channel==='JSAPI')invokeWechatPay(d.payParams);else if(d.channel==='NATIVE')showNativePay(d.code_url);pollOrder(d.out_trade_no)}catch(e){toast(e.message)}}
function invokeWechatPay(p){const run=()=>WeixinJSBridge.invoke('getBrandWCPayRequest',{appId:p.appId,timeStamp:p.timeStamp,nonceStr:p.nonceStr,package:p.package,signType:p.signType||'RSA',paySign:p.paySign},()=>{});if(typeof WeixinJSBridge==='undefined')document.addEventListener('WeixinJSBridgeReady',run,{once:true});else run()}
function showNativePay(codeUrl){$('checkoutModal').classList.remove('open');$('nativePayModal').classList.add('open');$('nativeQr').innerHTML=`<p class="notice">请使用微信扫码以下支付链接。生产环境可换成本地二维码渲染。</p><textarea style="width:100%;min-height:90px">${codeUrl||''}</textarea>`}
function pollOrder(outTradeNo){let n=0;const timer=setInterval(async()=>{n++;if(n>90){clearInterval(timer);return}try{const r=await fetch(`${API_BASE}/api/pay/status.php?out_trade_no=${encodeURIComponent(outTradeNo)}`),d=await r.json();if(d.trade_state==='SUCCESS'){clearInterval(timer);$('checkoutModal').classList.remove('open');$('nativePayModal').classList.remove('open');await track('payment_success',{order_id:outTradeNo,product_id:state.products.products[state.result.primary.key].product_id});toast('支付成功')}if(['CLOSED','REVOKED','PAYERROR'].includes(d.trade_state))clearInterval(timer)}catch(e){}},2000)}
boot().catch(err=>{console.error(err);document.body.innerHTML='<div style="padding:24px;font-family:system-ui"><h2>TYPE ME 初始化失败</h2><p>请检查 config/v2.json 是否可访问。</p></div>'});
